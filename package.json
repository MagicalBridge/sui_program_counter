{
  "name": "my-counter",
  "version": "1.0.0",
  "description": "TypeScript tests for Sui Counter smart contract",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tsc",
    "dev": "ts-node",
    "deploy": "ts-node scripts/deploy.ts",
    "deploy:testnet": "ts-node scripts/deploy.ts testnet",
    "deploy:devnet": "ts-node scripts/deploy.ts devnet"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@mysten/sui": "^1.36.2",
    "dotenv": "^17.2.1"
  }
}
